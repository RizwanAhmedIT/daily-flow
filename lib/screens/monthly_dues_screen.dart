import 'package:flutter/material.dart';

class MonthlyDuesScreen extends StatelessWidget {
  const MonthlyDuesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Monthly Dues')),
      body: const Center(
        child: Text('Monthly Dues Feature Coming Soon'),
      ),
    );
  }
}
